# 选佛谱 · 极简设计方案（统一规划）

> 2026-07-20 定稿。约束此后所有 UI/UX、美术改动；改此方案先改本文件。
> 总纲：**星图即棋盘，谱文即内容——界面只做三件事：导航、掷轮、读谱。其余一概退隐。**

## 一 · 设计原则

1. **一屏三区**：左杆（场景导航）· 右杆（十五门天梯）· 底栏（掷轮行动）。中央永远留给星图世界，不放常驻控件；顶栏两端（题字｜大厅·我的）是「去处」，不属三区之内（§七之六）。
1a. **单菜单原则**（2026-07-20 定，2026-07-28 收口）：全站只有一个菜单＝底栏「⋯」行谱菜单（手机底部抽屉呈现）。打开后先显示当前行处，只留两件局务（重开一局／退出，皆不可轻点）＋联机时的同修面板；右上角「大厅／我的」是常驻去处不是菜单，原文与设置收在「我的」里，屏幕上已有的入口不在菜单里再来一遍。
2. **色不过三**：金（#d7aa45 主）· 纸（#efe0b4 文）· 青（#96e1d6 辅，联机/提示专用）。法界配色只在珠色与色点上出现，不做大面积色块。
2a. **色 token 层（2026-07-29 色彩立宪）**：全站取色唯一出处＝`:root` 九枚 token——`--gold/--gold-deep/--gold-hi/--paper/--note/--teal`＋恶趣语义 `--woe`（深基·边底）/`--woe-tx`（红字唯一亮档）＋`--ink-on-gold`（主钮墨字）。明度分层用透明度档（金系两基座：底/边 rgba(215,170,69,α)、辉光/hover rgba(232,199,102,α)），**不再新造 hex**。豁免：canvas/three.js 侧色值（轮面/星流/珠色/realmHue）属数据层，归 JS 常量；JS 内联着色暂留字面 hex（写经纸兜底按 `[style*="#…"]` 字面匹配，token 化须与兜底改造同批）。**一屏一枚金主钮**：每卡至多一枚实心金渐变钮，其余降为描边幽灵钮。**浅面 token 族 `--aq-*`（2026-07-30 石青·晓，§七之十一）**：社交与个人面（大厅/动态/我的/茶寮/等候室面板）唯一取色处——页底/面板 `--aq-bg/--aq-panel`、青墨 `--aq-tx/--aq-title/--aq-note`、泥金 `--aq-gold/--aq-strong/--aq-goldline/--aq-goldwash`（只作线/形/洗底/大字，不作小字）、石绿 `--aq-green`、土红 `--aq-woe`、青墨线/洗 `--aq-line/--aq-wash/--aq-wash2`；浅面主钮＝金洗底＋青墨字＋金深描边。
3. **光即层次**：层级用透明度与发光强弱表达，不用边框粗细；大面积光斑一律 ≤0.42 透明度。
4. **字级五档**：`--fs-xs/sm/md/lg/xl`（11/12.5/14/16/19px）+ 展示级 `--fs-display`（22px）。全站不得出现散点字号；大字模式整体缩放变量。
4a. **字号豁免清单（2026-07-29 明示，此外一律走五档变量）**：①**展示级**——掷轮钮 48 · 落位大字 30 · 称念 29 · 题屏题字 36 · 见闻录大数 30/24（矮屏档）· 大厅/入座页题 24–28 与 clamp 形；②**大字距输入**——房号 22/28 · 密码 26（iOS 聚焦不缩放线为 16px，展示级输入尺寸另计）；③**微字签**——8.5–10.5px 竖排/组合字小注（改档易溢出，保留）。
5. **字族两用**：得意黑（SmileySans）为全站主字；展示级题字（面板标题/落位大字/门介）回退宋体系，气质不塌。
6. **谱义至上**：任何 UI 文案引用谱文必逐字（`scripts/crosscheck-pu.mjs` 护栏）；界面装饰不得遮谱文。

## 二 · 布局架构（一屏三区）

```
┌──────────────────────────────────┐
│ 题字(左上)        [大厅][我的](右上) │
│ 场                             天 │
│ 景  ← 中央星图世界（唯一主角） →   梯 │
│ 导                             十 │
│ 航                             五 │
│ (左杆·场景内才现)              门 │
│                                  │
│      [⋯]  长按掷轮  [问]   (底栏) │
└──────────────────────────────────┘
```

- **右杆天梯**（常驻）：十五门刻度+现居门发光；门号一律横排（nowrap）。
- **左杆场景导航**（入场景才现）：色界＝四禅四签；菩萨道场＝十科签。色点+名，无底无框，选中亮点放大。与右杆同语言。
- **底栏**：左「⋯」谱务 · 中「长按掷轮」 · 右「问」。联机时左下加同修脉签。
- **行谱菜单＝唯一菜单**（2026-07-28 收口，此段取代旧清单）：
  顶部只显示当前行处；条目只余 ①同修面板（联机时）②重开一局 ③退出——两件局务皆是不可轻点的危险操作。
  全谱走右侧天梯、行迹走上一掷轮相牌、大厅与我的在右上角、原文与设置收在「我的」里；屏幕上已有的入口不在菜单里再来一遍。
  裁撤出菜单：廿一因（因地门介仍可达）· 观照/收藏统计行 · 独立「归位」「参考经典」条目。自由观照底坞同样保留「⋯」，设置不上题屏（取舍：题屏是开局引导，不承载个人面；回访者一 ✕ 即达右上「我的」）。
- **探底剖面杆**：只在娑婆全图存在；入门/入场景/入净土一律隐。

### 2.1 共修大厅（2026-07-26）

- 题屏无存局时只给一个主按钮「开始行谱」，进入大厅后再选择「一人行谱／与人共修」，避免题屏和大厅重复分流。
- 大厅使用全屏壳，不再是窄弹窗；顶部依次为在座状态、滚动共修动态、单人／多人两个入口。
- 「与人共修」执行智能入座；十二室保留为次级手动选择。桌况八秒局部补写，不得销毁重建大厅，以保留滚动、焦点和榜单状态。
- 一个人同一时间只进入一间共修室；重复点击只执行一次入座，换室必须先确认旧座已经释放。
- 共修室不向用户显示东南西北方位。最先入室者为房主，房主离开后按入座次序递补；两位准备即可开始，第三、第四位不是开局条件。
- 手机房间面板中，「离开」「准备／开始」须固定在首屏可见区域；聊天内容只占剩余空间滚动，不得把关键操作挤出视口。
- 点滚动动态打开「共修动态」独立全屏页（原「念佛功课榜」内层弹窗制已被 §七之六 取代）：一掷一称念，按最近用功先后列出、**不排名次**；匿名身份在本机稳定保存，同名莲友不合并（服务端缀莲号尾四位区分）。只作随喜记录，不作修证高下。
- 换室须等待服务器确认旧连接已关闭再入新室；普通离席与静默换室使用不同回调路径，禁止用固定毫秒延时猜测座位是否释放。

## 三 · 15门220位 交互规范

- **掷轮**：长按至心称念→松手旁掷（占察轮 3D 实转）；未轮到（联机）按钮压暗不可掷。
- **判词窗**（每掷必出，中央下方）：`轮面二字 → 方向徽 → 去向位名（展示级字）→ 谱曰缘由（可展开原文）→ 行▸`。点正文展白话+原文；下滑收签、上滑唤回。
- **行棋**：彗星光弧乘行，跨门先拉远同框再进；落位金环涟漪+位名大字（展示级）。
- **位次浏览**：点门星展本门位珠；位珠点开＝谱注抽屉（原文+白话+出处行号）。
- **不打断原则**：一切浮层可点外关闭；行棋动画可点跳过（设置「行棋特效」关＝自动直达）。

## 四 · 十法界世界美术规范（极简）

- **须弥山**：敦煌细腰旋成体，四宝四面（南琉璃/东白银/北黄金/西颇胝迦），金线描边为唯一描边语言。
- **七金山七香水海**：金脊环（皆金所成，双金调相间）×7，环距内山基不叠压，海环必可见。
- **四洲**：东半月/南车箱/西满月/北方座+各二中洲；洲色映本面宝色，昼夜随日。
- **色界四禅**：光盘+星环，盘晕 ≤0.42、侧晕减半；成员星只在该层绽开时亮名。
- **无色界**：无方所——只以轴顶微光与名示意，不做实体建筑。
- **净土**：横轴西方（横超光路），敦煌壁画底幕+莲池双层金碧微光。
- **降噪三禁**：禁常亮大光斑、禁多层重叠辉光、禁与谱文争眼的动画。

## 五 · 卡片（overlay）统一体系规范（2026-07-21 重定，v225–v236）

> 用户三条定案连成一套统一卡片语言：①**白话为主体，原文为依据**（不再以繁体原文当主句）；②**一卡一序、体例同构**（全站卡片共用一套排版类与结构次序）；③**每张卡都能回到局中**（局中态主钮＝「回到局中」，非局中＝「关闭」）。

### 5.0 命名（2026-07-21 用户定案·大白话原则）
- 「谱曰原文」一律改叫 **「原文」**；「对读」一律改叫 **「文白对照」**。原则：用大白话让用户一眼明白是什么，术语让位于清楚。

### 5.1 统一壳与出场
- 壳：普通内容使用 `.panel`（圆角14 / 边线金 26% / 磨砂）＋遮罩；手机端行谱菜单／联机类走**底部抽屉**。共修大厅是模式选择中心，例外使用全屏不磨砂壳。
- 一屏一窗：新窗必关旧窗；✕ 与蒙层点击等效；场景转场用 `softCloseOverlay`（不吞 `.keepOv` 重要面板）。

### 5.0b 内容极简·信息只出一次（2026-07-21 用户定案）
- **不重复信息**：同一信息、标签、内容全站只显示一次。门名只在**顶签**出现（位卡「解读」不再重述「所在门：第X门『门名』」）；位名只在标题；「所在法界」仅当法界名 ≠ 位名时才显（免 无想天/无想天 自我重复）。
- 位卡去冗：撤「AI 解读」折叠壳与「本门总说」折叠（门总说原文经顶签「本門譜文 ›」入卷阅读，不在每张位卡重复）；门大意收成一行**键值**（`.cKv`：小金键「本门大意」+ 白话值），不再叙述式重述。
- 「问」聊天卡撤底部说明脚注（用户点名删）。

### 5.1a 双主题（2026-07-21 用户定案）：暗夜（默认）/ 写经纸（可切）
- **暗夜用底色渐变作分层**（用户点单）：`--ck-panel` 改 `linear-gradient(168deg, 顶 rgba(40,33,64) → 底 rgba(16,13,30))`，卡片有纵深，段落坐在渐变上而非一片死黑；写经纸同理用暖渐变（纸有厚度）。层次＝渐变底 + 原文沉入凹槽块 + 文字三级明度，三管齐下。
- **问题**：旧暗底把白话/原文/对照/注脚都做成暗底上的中间调，糊在一起、不好读。**两手治理**：
- **① 暗夜主题（默认·层次重做）**：白话提亮到近白（`--ck-plain` #f6ecc6）、解读降一档、注脚最弱，三级明度拉开；原文进**深凹槽块**（`.verse` bg `rgba(0,0,0,.26)`+金左线）与白话分开；白话对照石绿提亮（`--ck-bai` #93cbac）并标「白」。
- **② 写经纸主题（可切·最好读）**：暖浅底墨字——卡底 #f7efdc，正文墨 #2a2318，原文沉香墨 #4a3d24 沉入凹槽块，白话对照石绿 #2f6a52。卡片浮于暗世界之上如展卷。
- **实现**：全套卡片色走 `--ck-*` CSS 变量；`html.paperCards` 整体切换（`save.cardTheme='night'|'paper'`，设置卡「卡片主题」开关，`applyCardTheme()` 启动应用）。**只切 token 不改结构**。世界标签/控制台/掷轮钮不在卡内，不随主题变；写经纸模式对旧卡片内联浅色有 `[style*=]!important` 兜底映射，保证纸上可读。

### 5.2 排版体例（六个类，全站统一；色走 `--ck-*` 双主题 token，字级归五档变量）
| 类 | 用途 | 暗夜 / 写经纸 |
|----|------|------|
| `.cMeta` | 金字顶签（门次/位次/卷次/分节标题） | 亮金 / 泥金 |
| `.cPlain` | 白话主体首行（「这是什么」一句） | 近白 / 墨 |
| `.cRead` | 解读层（在何门·何界·为什么） | 米黄 / 沉墨 |
| `.verse` | **原文**（沉入凹槽块，宋体） | 沉香黄+暗块 / 沉香墨+浅块 |
| `.dd` | **文白对照**白话对句（标「白」，缀原文段下） | 石绿亮 / 石绿深 |
| `.cNote` | 灰字注脚（出处体例/操作提示） | 灰金 |
| `.lnk` | 点读虚线（可点入原文/法界/卷文） | 随主题 |

### 5.3 位卡三层一序（`openSfpNote`，v228 定 / v237 改名）
自上而下恒定：**顶签 `.cMeta`（门·第N/220位·卷次点入）→ 白话首行 `.cPlain` → 「解读」折叠（所在门＋所在法界＋是否毕局，`.cRead`）→ 「升降行法·廿一组轮相」折叠 → 「原文 · 文白对照」折叠（`duiduHtml`：从 `SFP_CANON_DOORS` 读取该位完整谱注，白话库键逐字嵌入原文；命中段「原文在上 `.verse`、白话对句在下 `.dd`」，出处 `.cSrc`）→ 「本门总说」折叠 → 上/下位翻页 → 主钮**。
- 原则：**先白话后原文、先大意后逐字、先解读后依据**；引文皆逐字繁体（CBETA B0136），文白分层不混、两种颜色两种声音。

### 5.4 判词卡（`showVerdict`，v226 定）
- 白话为主句（`#vWhy`）；命中原文库时缀「谱曰原文 ▸」（`.vsrcT`）点开小字原文（`#vSrc`），不再有独立工具行。
- 去处位名（`.vdst`）**可点弹白话小签**（`openPosGloss`：位名＋一句白话＋「原文说明 ›」入位卡）。
- 「AI 解读」小签（`.vaskC`）缀在正文同行，点开＝直达本掷的既定解读内容卡（本地检证拼证，非聊天）；卡内可另点「问」进入追问。

### 5.4b 「问」参考 wenchao 问文钞（2026-07-21 用户点单，NotebookLM＝谷歌 NotebookLM）
- 参照 `lianbang999/wenchao` 的 `/ask` 与 `ai-core.js`：**据文检证 + 可点出处**的问答范式（谷歌 NotebookLM 式——答语只据所供语料、句句挂出处、点角标看原文摘录）。
- 已采：**智能体三段式检索反馈**（`.cbStage`：「正在检证 G 版经据库」→ 过 5 秒「正在细检 G 版相关篇章并综合」，仿文钞 slowTimer，免十余秒静句被当卡死）；输入框跟随主题（`.cbInput`，16px 防缩放）；撤底部说明脚注。
- 每次普通提问直接接现有智能体（`/api/ask`），答复带出处、明标 AI 生成、每人每日 100 次；本地检证同步生成，作为等待期间速查及连接失败兜底。

### 5.5 「问」＝与本谱对话（聊天式，v236 定）
- 聊天双气泡（问右 `.cbU` / 答左 `.cbA`）＋ 快问签（`.chipQ`）＋ 输入行（16px 防 iOS 缩放）。
- 与「AI 解读」严格分家：打开「问」不自动灌入上一掷解读；默认 3 个快问随局势生成——当前谱位、最近轮相、所在门各一问；未开局时显示玩法、六字轮相、上品上生三问。
- **G 版智能体主答**（2026-07-22）：普通聊天每一问都直接交自建 Cloudflare 智能体（`/api/ask` → `ASK_SERVICE` → `xuanfopu-evidence-agent`），其经据索引来自 `选佛谱G版（codex）/agent/corpus/rag-v2`；答复以气泡回贴，**明标「AI 生成」与引文出处**，本机缓存同问即答。结构化本地检证（逐字谱曰·行法表·813 句缘由白话对读库·220 位白话·词典）保留为等待/离线兜底，并折叠在智能体答复下供核对。
- 聊天输入框占位提示与快捷问题签统一使用全局字体变量 `--f-ui`，不再由浏览器默认输入字体造成视觉跳变。

### 5.6 卡片「回到局中」通则（v221/v236）
局中（`sfpS.active`）时，行迹/消息/全图/原文/净土/廿一因/选佛榜/谱务各卡主钮一律「回到局中」；非局中为「关闭」。谱务菜单亦补「回到局中」大钮。

## 六 · 数据护栏（对齐原文）

- 底本：CBETA B0136《選佛譜》六卷（`选佛谱G版/繁体版/`）；人工核对层：`xuanfopu-h5/data/positions.json`。
- 护栏命令：`node scripts/crosscheck-pu.mjs`——220 位去向/起手/门题/谱曰须全零差异方可发版。
- 2026-07-20 已修正三处历史错误（均经原文逐字裁定）：
  1. 上品上生「佛佛」→ 極果位（原误：常寂光淨土位）——原文「佛佛。寂光上上」
  2. 實報莊嚴淨土「彌佛/陀佛」→ 極果位（原误：停原位）——原文「皆實報上上品」+谱曰「除上上品入極果位」
  3. 常寂光淨土「阿佛/彌佛/陀佛」→ 極果位（原误：停原位）——原文「皆寂光上上品」
  另：终局位显示名对齐原文首行「佛」（圓教究竟妙覺位为副题；id 不变，存档兼容）；220 位谱曰全量回填人工核对底本（保留原文夹注与科判结语）。

## 七 · 联机方案（极简定案）

**当前定案**：公共大厅采用每厅 12 张固定共修室，每室至多四人；可直接择空座，也可由房主设置四位密码后以邀请链接邀熟人。

联机三幕：**大厅 →（入房）等候室 →（开局）脉签+聊天**。

- **大厅**（题屏「联机同修」进入）：名号（一次填写永久记住）→ 大按钮「开新房」；或房号+入房；下方「最近的房」至多三枚（实时显示 在线/在房 人数、已开局），点即再入。
- **等候室**（入房未开局的主场景）：房号大字（点击复制）· 四座横排可视化（实座＝珠色圆珠+名号，房主带「主」标，离线淡去；空座＝虚线圈「虚位」）· **情境主按钮**（独自→「邀请莲友·点开即入」；房主且≥2人→「开局·N位同修」；非房主→「候房主开局·可先邀莲友」；四座已齐非房主→置灰候开局）· 细字行「聊天·离房·收起」。开局即自动散场；点蒙层/收起可暂离（脉签点回）。
- **同修脉签**（原「聊」浮钮升级）：左下小胶囊内**四座色点即名单**——谁在房一目了然；开局后**轮到谁谁的点脉动发光**；离线点淡去；未读角标照旧。点开＝未开局回等候室、开局后开聊天。
- **房号＝4 位数字**（2026-07-20 定，为微信/社交传播设计）：口头可报、语音可念、手机可拨。旧字母房号与旧深链仍兼容（正则收 `[A-Za-z0-9]{4,8}`）。
- **邀请＝分享卡**（一个组件两用）：带房号＝邀请卡（房号大字＋**二维码**＋「转发给莲友」主钮）；无房号＝荐游戏卡（题屏细字行「分享」）。二维码内嵌生成（无外链库），面对面场景莲友扫屏即入，零输入。转发走 `navigator.share`（系统分享面板）；**微信内置浏览器无 share**——检测 `MicroMessenger` UA 时改出「点右上角 ⋯ 转发给朋友」引导＋复制文案兜底。等候室常显小二维码。
- **一键入室**：深链落地后进入对应准备室，不自动开始个人谱局；本机无名号时只补问一次。
- **共同再局**：联机中没有个人重开；本局共同结算后，各自点“准备下一局”，房主统一开局并轮换首位。
- **聊天**：名单与聊天同面板；快语一键即发。手机端为底部抽屉：**等候态默认全高（留缝可收），局中默认半屏**，上滑/⤢ 全屏自取（2026-07-30 修宪，原「半屏抽屉」明文改此）；桌面头行 ⤢ 钮全屏/还原。点面板外任意处、点标题空白、按 Esc 或下滑均立即缩小，第一下点击不穿透到游戏。
- **轮次**：开局后按座次轮掷；未轮到者掷轮压暗、脉签与名单亮当前者，脉签字位直报当前操作者（轮到自己作「该您」）。
- **贈掷＝施与同席**（2026-07-27 定稿，grant-ontology v2.0.0）：掷得「贈N掷」者不自留，判词行毕出择人卡，择一位同席莲友受之；受赠者在自身所在之位续掷，队列用尽才轮转下一位。三十秒未择按座次自动施与；无人可受（含一人行谱）则此贈作废。此为**本项目定稿操作规则**，非原谱逐字规定，判词内一律走 `operational_interpretation` 标注。
- **本手限时可见**：在线一分钟、断线三十秒、判词兜底一分钟、择人三十秒，随 `room.turnDeadline` 下发；掷轮钮剩二十秒内、判词卡剩三十秒内显示秒数。连续两手未掷即「暂离」，掷轮钮与面板均给「归队」出口，不必刷新页面。
- **等候室层级**（2026-07-27 极简重构）：状态行报事实（几人在线／几人已准备），按钮上写下一步（「共同开局 · N 人」），按钮下一行小字只说该做什么——原先带框的两步图与状态行三处说同一件事，已撤。聊天区不另立标题，「仅本室可见」并到该行右端，翻历史时借同一位置报新消息。
- **开局权**：房主自然可开局；房主离线、或人已齐备满四十五秒，任一已准备者亦可开局，房主挂机不锁死全房。
- **共同结算**：及第者出及第面板（内含同座现况），其余人与旁观者出**共同结算卡**（各人掷数与行处、中止原因、准备下一局／离席／观照三个去处），两者不并出。
- **大厅极简**（2026-07-27）：十二格中空室只留一个淡序号，有人的室才长出点阵与名号——两套记号说同一件「没人」，反把「哪里有人」淹没；共修动态只呈最新一条静态文本（原 34 秒无限跑马灯常驻烧帧，违 §七之二）；右侧「今日共修／入座后／邀请说明」三块已撤，数字归功课榜、入座说明归房内指引。
- **断线**：自动重连回原座、棋况保留；离线者名单置灰不除名。

## 七之三 · 进入与退出主干（2026-07-28 理顺）

一条主干，节点各只有一处入口，且每一处都进得去也出得来：

```
题屏 ─┬─ 开始行谱 → 共修大厅
      └─ ✕ ──────→ 自由观照（星图）

自由观照底坞：[续掷／选佛] [大厅] [同修色点] [⋯]
                └ 有存局说「续掷」，无存局才说「选佛」——一钮一义
                   「大厅」常驻：从前观照期进大厅只能走二级菜单，而主钮在有存局时直接续掷、根本到不了

共修大厅 ─┬─ 一人行谱 ────────→ 局中（单机，不问名号）
          ├─ 与人共修·随喜入座 ┐
          ├─ 十二室点选 ───────┴→ 准备室 →（共同开局）→ 局中（联机）
          ├─ 共修动态 → 念佛功课榜 → 取名／改名
          └─ 返回：有活局回局中，无则回题屏

局中底栏：[⋯] [掷轮] [聊] [问]
局中「⋯」：全谱·行迹·共修大厅 ｜ 见闻录·原文·设置·重开一局·**退出**
             退出＝行处存档后回题屏（联机则先确认离席）

终局 ─┬─ 及第者 → 及第面板
      └─ 其余人／旁观者 → 共同结算卡
        两张卡的去处一致：准备下一局 ／ 离席回大厅 ／ 观照星图；
        **手关（✕／点背景／滑动）一律等同「观照星图」**——从前只有卡上的钮会收尾，
        手关则唱赞照唱、乘光流星挂在天上、局面停在「活局在终点」的僵尸态。
```

- **唱赞可停**：`playChant` 留住句柄，`stopChant()` 在收局、开新局、离开及第面板时统一止住；从前起了就撒手，只能去设置里静音。
- **及第面板兜底**：转场若迟迟不结束，最多等六秒也放面板出来（并收掉流星），不让人对着一颗不动的流星干瞪眼。
- **占房租约只拦「换到别室」**：服务器本按 `clientToken` 认人，同室重进只是接回原座。租约缩到 15 秒，且被拦时一律问一句「那个页面已关了？接管入座」——微信里每次从聊天点开都是新 WebView、旧的常留后台不发 `pagehide`，两分钟硬租约会把人直接锁在门外。
- **功课记名据实告知**：一人行谱不问名号，功课因此记在匿名莲号（`莲友·XXXX`）下。功课榜里标出「您」那一行，并直说「记在 X 名下」，旁附取名／改名入口——从前人翻遍榜单找不到自己，只当没进榜。

## 七之四 · 一屏四角与控制台（2026-07-28 重设计）

**四角各管一件事，中央永远留给星图**（§一）：

| 位置 | 内容 | 作用 |
| --- | --- | --- |
| 左上 | 题字「选佛谱 ⊙」＋状态小签（娑婆／全图／归位） | 观照全图 · 回到现居 |
| 右上 | **大厅｜我的** | 公共面／个人面两枚平级去处（常驻，观照期与局中皆在） |
| 左侧 | 探底竖杆 ＋ 档位（全图／门场／星位） | 视角 |
| 右侧 | 天梯（十五门 因→佛） | 进度 |
| 底部 | 掷轮控制台 | 行棋 |

**控制台＝两行制**（原为六层，其中四层是死的）：

```
[上一掷 陀佛]  现居「見取」· 第 12 掷            ⊖   ← 状态行：点位名开位卡／长按飞回棋子；
                                                     点轮相牌开行迹；棋讯瞬时借同一行说话
[      长 按 掷 轮      ] [聊] [问] [⋯]              ← 操作行
```

清掉的死层与其缘由：
- `#sfpTop`（门名＋掷数）——被 `display:none!important` 钉死，却每次 `sfpStatus()` 都在计算。
- `#sfpDoors`（十五门小圆点）——与右侧天梯报同一件事，且从未显示（§5.0b 信息只出一次）。
- `#sfpName`（现居位名）与 `#sfpMsg`（棋讯）——从未显示。`sfpShowMsg` 一直往看不见的元素里写，
  「消息回看」因而也无处可点。现二者合并为状态行：常态显位名，棋讯来时占用 5.2 秒再退回；
  显棋讯时点它即开「消息回看」。
- 「收起」钮从半悬在上边框外收进面板内角，状态行右侧留位让行。

大厅入口从底坞移到右上角：底坞（自由观照期）复归三枚——[续掷／选佛] [同修色点] [⋯]。

## 七之五 · 功课与共修动态（2026-07-28 重定）

**念佛计数的语义是一辈子只增**，故功课改为累计制，不再按天清零（与 foyue 念佛计数器同口径：累计 N · 今日 N · 连续 N 日，「今日未计则从昨日起算，不因今天还没念清零」）。

**公共面在大厅，个人面在「我的」**，两者不再混在一张卡里：

| | 呈现 | 数据 |
| --- | --- | --- |
| 大厅顶条 | `本站共修第 N 天 · 已参加 N 人 · 累计掷轮 N` ＋ 最近三位 ＋「共修动态 ›」 | `plaza_counter` ＋ `plaza_practice` |
| 共修动态 | 一人一行：名号 · N 掷 · 及第 M · 何时。**按最近用功时刻倒序，不列名次**——念佛记录上不比高下，时间先后本身即次序 | `plaza_practice ORDER BY lastAt DESC` |
| 我的 | 四格表（全站累计／全站今日 ∥ 我的累计／我的今日，**上下同列对照**）· 共修 N 天 · 连续 N 日 · 及第 N · 月历（格内日期、格下当日掷数、可翻月）· 行谱见闻（本机）· 我的行谱记录（逐局） | `/api/plaza/me` ＋ 本机 `save.lg` |

- **存储**：`plaza_practice(莲号, 名号, 累计掷轮, 累计及第, 共修天数, 初次, 最近)` 为主；
  `plaza_daily_practice` 只供月历与连续日，保留期 32 天 → **400 天**；旧每日数据一次性汇总迁入，一声不丢。
  `plaza_runs` 加 `actor` 列，及第自此归到本人名下（共修室的由房间随 `join` 带来的莲号上报）。
- **措辞**：主字用「掷轮」（直白无歧义），法义由一句「一掷一称念『南无阿弥陀佛』」承担，不在正文反复申明。
- **说明只留一句**：`一掷一称念…只作随喜记录，不作修证高下。功课记在本机莲号下，换设备会另计。`
  原先四小句、口径混杂的说明（今日称念／今日莲友／今日及第／累计称念并列，末一个其实是全站量）全撤。
- **未决**：莲号绑本机，换设备即另计一人——「累计」这个承诺因此只兑现一半，
  「已参加 N 人」也会因换机而虚高。功课码导出／导入尚未做，页面上已如实告知。

## 七之六 · 三张全屏页与两枚去处（2026-07-28 定）

**星图是家，右上角两枚去处，左上角是观照。**

```
星图 ── 左上：题字（观照全图）＋ 状态小签（娑婆／全图／归位）
        右上：[大厅]  [我的]        ← 公共面 ｜ 个人面，两者平级
        底部：掷轮控制台 ·[⋯]
```

「我的」不放进大厅：二者是平级的两个去处，放进去就成了公共面的子页，语义拧了；
且局中想看今天念了多少，不该先要「进大厅」这个换地方的动作。

**三张全屏页同壳**（`.pzPanel > .fsShell`：同一张底、同一处 ✕、同一套留白）：

| 页 | 从哪进 | 内容 |
| --- | --- | --- |
| 共修大厅 | 右上「大厅」 | 顶条叙述 · 一人行谱／与人共修／茶寮 三卡 · 十二室 |
| 共修动态 | 大厅顶条 | 一人一行，时间序，无名次；「回大厅」一条退路 |
| 我的 | 右上「我的」 | 功课四格表 · 月历 · 行谱见闻 · 行谱记录 · **六卷原文** · **设置** |
| 莲友茶寮 | 大厅三卡之三 | 与佛悦主站共修群相通（/api/cmt 同数据不同皮）；「回大厅」一条退路（2026-07-30 修宪：三页扩为四页） |

- 共修动态从大厅的内层弹层改为独立全屏页：少一层嵌套，退路也只剩一条。
- 原文与设置收进「我的」（与 foyue「我的」页同一模式：概览 ＋ 若干行入口 ＋ 设置）。
  原文在位卡、门卡里另有入口，此处是「只想读经」时的直路。
- **全屏页与同修面板不并存**：`#netPanel` 的 z 高于覆盖层，进任一全屏页前先收起面板，
  看完点「聊」唤回。

**「⋯」收为两件局务**：重开一局 · 退出（都是不可轻点的，故仍藏在菜单里）。
全谱走右侧天梯、行迹走上一掷轮相牌、大厅与我的在右上角——屏幕上已有的入口，菜单里不再来一遍。

## 七之七 · 图标层（2026-07-28 定）

**先认形，再认字。** 去处与行项一律「形在前、字在后」，形只作辨识，不独自承担语义。

图标一律内联 SVG（`src/icons.js`），不引任何图标库——离线可用、无第三方请求、不受 CSP 限制。
规格统一：24 见方、只描边不填充、粗细 1.6、圆头圆角，**颜色一律 `currentColor`**，
跟着所在按钮的文字色走：不必为亮暗两套主题各配一份，也不会多出第四种颜色（守「色不过三」）。
尺寸由 `.ico` 用 `em` 决定，跟着字号缩放。

| 处 | 形 | 何以是这个形 |
| --- | --- | --- |
| 大厅 | 殿门 | 大厅是个「去处」，画门最直白 |
| 我的 · 一人行谱 | 人 | 同一个意思用同一个形，不另造 |
| 与人共修 | 三人 | 与「一人」并排时，一眼分得开 |
| 行谱见闻 | 眼 | 记的是「所见几位」 |
| 六卷原文 | 经卷 | 册页与行 |
| 设置 | 推子 | 比齿轮安静，也更贴近声音／简繁／卡片这类挡位 |

**窄屏留形去字，不是留字去形**：字最挤的地方反而最需要那个形。
- 星图两枚去处（≤380px）：收字成 36 圆钮，只留形；`aria-label` 保留，读屏与长按提示不受影响。
- 大厅两张模式卡（≤640px）：脱掉圆环、只留字形移到标题左侧，卡高不变（不多占一行）。

## 七之二 · 功耗规范（v221 发热治理，2026-07-20 移植）

- 触屏机（`pointer:coarse`）默认省电档：DPR 上限 1.6（桌面 2）；泛光缓冲减半分辨率。
- 静观期 30fps：飞行/转场/掷轮/触控输入/远端珠滑行等动势期放行全帧率；交互后 1 秒提帧窗。
- 自适应降档：30fps 仍撑不住（帧距 >45ms 持续约 7 秒）则 DPR 再降 15%，只降不升防振荡（下限 0.7）。
- 探针：`__dbg.perf` 读 `{isCoarse, dprScale, pr}`。新增动画必须自问「静观期是否在烧帧」。

## 八 · 已实施 / 待办

**已实施**：左杆场景导航（色界/菩萨）· 弹窗统一壳减重 · 色界盘面降噪 · 观照场隐探底杆 · 字级五档+得意黑 · 天梯横排 · 数据对齐（0 差异）。
**2026-07-21（v59–v61 复刻＋卡片统一）**：卡片五类体例（`.cMeta/.cPlain/.cRead/.dd/.cNote/.lnk`）全站铺开 · 位卡三层一序（白话→AI解读→谱曰对读）· 判词卡白话为主·位名点弹小签·AI解读小签 · 「问」升级为聊天式两层作答（本地检证＋深问接智能体） · 色界聚显两态（他层整层隐·光云盘随层呼吸·五净居金白圣色·禅签禅支小字） · 全卡「回到局中」通则 · 220 位白话（`sfp-pos-plain.js`）与 813 句缘由对读库（`sfp-why-plain.js` 扩至 574 键）随上游同步。验证：build 绿 · 模拟 200 局全圆满 · crosscheck 220/220 零差异 · 联机 22/22 · 浏览器实测（位卡三层/问答四型/深问签/掷轮落净土）。
**2026-07-22（复刻 MakePlay V66）**：位卡「原文 · 白话文对照」改从六卷权威原文 `SFP_CANON_DOORS` 读取完整谱注，不再使用简化 `note`；法界主星加入恶趣焦岩／人天云涌／色无色冰玉／四圣金脉琉璃四段灰度纹理、菲涅尔边缘光与极缓自转，同时下调清漆反射及广告牌光环，降低光污染。
**2026-07-22（复刻 MakePlay V67）**：「AI 解读」固定补齐是什么／为什么／怎么修行三问，直接采用十五门成熟修行白话；掷轮解读卡增加当前位、所在门修行、六字轮相三个情境追问签，点击后带问进入独立聊天功能「问」，两项功能仍分家。
**2026-07-22（复刻 MakePlay V68）**：星图进一步降噪并重排层级——日月与极乐星改为单层静态柔光，四圣依次采用水晶、琉璃、银、金材质，菩萨道场改为由暗至明的银阶、放大位珠并撤场内光晕；顶栏题字改为直入全图观照，「⋯」谱务移除重复的观全图／归位入口，续掷统一由「选佛」进入。
**2026-07-22（复刻 MakePlay V69）**：四圣星体的几何层级继续细化为声闻 8 面、缘觉 20 面、菩萨 80 面、佛圆球，位阶愈高愈趋圆满；AI 解读中的原文依据改用“这一掷为什么这样走”“现在／要去的位置原文怎么说”“这个组合为什么这样走”等白话折叠标题，书名卷次保留为弱化出处小注。
**2026-07-22（复刻 MakePlay V71）**：承接 V70/V71 稳定版累计更新——菩萨银晶刻面、升沉轴渐变与观照档位提示；判词方向染色、天梯联闪、贈掷徽标与长按进度；乘光轨迹按来去门色渐变、熟路加速、落位涟漪、残迹点读和行迹连线；手机小珠/门星扩大拾取、桌面悬停名签、滚轮退档与键盘巡门；AI 解读增加行程走势、重访减量、“同一轮相，因位异行”及情境预判追问。成佛及第时新增独立可关闭的阿弥陀佛唱赞，延后于磬声播放一遍，不恢复背景循环音乐。
**2026-07-22（公共选佛榜第一版）**：第一次进入榜单先填写最多十二字的榜上昵称，提交后立即加入全站共修榜并固定显示“我的行谱”卡；以稳定莲号区分重名同修，支持改名不重复、最近参与排序、分页查看更多，并保留同房莲友进度。公共数据由现有 SQLite Durable Object 命名空间中的固定全局对象汇总，不新增外部绑定。
**2026-07-28（同步 MakePlay V96 · 只取内容）**：本地内容基线原为 V90，此次只前移四个纯数据文件，`game.js` 等分叉代码一律不动。① **出处补「义」字** 47 处——「俱舍论卷十一」改作「俱舍论卷十一义」，把「依义撮述」与「逐字原文」在出处一栏就分开标，不再让读者误以为是原经字句。② **名相小词典 141 → 291 条**，并把三处词形改回与原文一致（四加行→加行、念佛三昧→念佛觀、持名→持佛名）。③ **位释改依谱文**八条：初發心住、二治地住、六正心住、七不退住、三精進心、九戒心、十願心、五難勝地，由泛泛套话改为依本谱行文，并注明「谱准位同」的判位来源。④ **行法白话补 31 条**（上游机审「有行法原文而无白话」的缺口），另订正一处键名——`圓人善超。一生能頓入故` 旧键多一个句号，在 `sfp-data.js` 中零命中，那条白话此前从未显示。上游 `sfp-data.js`／`sfp-canon.js` 与本地逐字节相同，原文与谱注无改动。本地 `sfp-why-plain.js` 的 47 条助读追加与上游新增键零撞车，原样保留。

**待办**：菩萨道场中央莲台模型与金光转场 · 四恶趣依经重定位（地狱/饿鬼真正移至南洲下，阿修罗移至山北海下）· 无色界轴顶微光 · 判词窗内容层再精简（白话默认、原文点开）。

## 七之七 · V97 增量并入与大厅修缮（2026-07-29）

上游 makeplay V90→V97（v339–v366）经三方基准法逐项核对后并入；本地已有等价实现者（见闻录、门要、
证据分层等）不重复移植。本轮并入：

- **诚实性**：撤心相三条量表（数值系自撰非经论所载）；坐标类型两名制（「依经有处／非方所摄」，
  schematic 并入 scripture，异说不再上卡）；节点卡「异说并存」段撤（数据留档不呈）。
- **v364 二百二十位全归主图**：DISC_DOORS 清空，门1 因地环／门2 沉沦链外推／门9 各法界前庭低弧；
  谱页退为深读——天梯久按 460ms 或门要卡「观本门谱页」进入。
- **行棋节奏**（v360/361）：第 6 掷起旋时与定相停留收紧（仪轨不减）；判词在场按掷钮＝先落子再起新掷，
  掷钮题字随判词换「行 ▸」；长途飞行弧高与时长改 sqrt 增长，升沉看得清。
- **读谱**（v354/355/356/363）：谱文页加位名目录横滑与逐位文白对读（overlay 内跳转用 scrollTop 差值，
  scrollIntoView 在裁剪层内不动）；位卡改四事直陈（双链眉行·地理坐标行·白话·本位原文对读），
  行法按语与位注切分不重复；hover 名签补门属，触屏首局一次性位珠引导。
- **解读卡**（v348/350/351/359）：去向条＋标签短行制；「问」随问携带局面上下文（现居位/门/轮相/足迹），
  本机缓存键并入现居位；离线兜底新增「为什么这样走」缘由专答；过程自述文案全撤。
- **大厅修缮**（本地捉虫）：openPlaza 代号护栏修幽灵定时器；刷新偶发失败不再静默永停；
  ✕/Esc/背景/右滑与「回题屏」钮同去向；上锁室问名后直问密码（不再先白打一次失败请求）；
  「我的→改名号」同路往返回「我的」；重名「您」标注按莲号尾四位精确比对；
  局中经大厅/兜底钮「一人行谱」补掷中护栏与丢局确认；worker alarm 清座修 turnIdx 重映射，
  webSocketClose 名单排除正在关闭的连接，大厅满座尺与 join 同尺（含断线保座者）。
- **图标**：描边 1.6→1.75（14px 档不再亚像素发灰）；sliders 轨道与旋钮负间隙修正；窄屏两钮热区补足 44px。
- **收尾两小项（同日晚）**：①简繁切换 title/aria-label/placeholder 属性同转（zhAttrs：记「原文＋上次写出值」，程序改写过的以新值为原文），并按界面语料扫描补齐裁剪字典缺字（题/厅/课/码等；「准/舍/系/制/云」歧义字不作单字映射，谱文「准」原字守逐字；词组 日历→日曆 先于单字；谱文异体字仅 T2S 简读）；②安卓返回键/iOS 侧滑接管（armBackGuard 哨兵历史项：确认卡→浮层→聊天面板→门观→专场→局中缓退 逐层退出，退无可退才离站；局中裸退两按确认「行处已存」）。回归脚本 scripts/test-back-and-lang.mjs。

## 七之八 · 极简重构五批施工（2026-07-29 夜，依《极简重构方案-20260729提案》39 条定谳）

**第一批·止血（行动线与浮层秩序）**：
- 判词卡空「释义：」绝迹：`makeSfpInterpretationEvidence` 空文本不造项＋渲染层双滤；**首掷判词释义＝廿一因义读＋本位白话**。
- 键盘与返回键补契约：浮层开着 Enter 不再隐提交判词（补 `!overlayEl` 守卫）；**判词在场按返回＝落子收层**（与掷钮 v360 同义），「行处已存」不再失实；Esc＝判词收签（与下滑同义）。
- 弃档三入口统一确认等级：题屏「新开一局」补确认；大厅/兜底钮「有存局未在局」同样确认。
- **浮层期顶栏「大厅/我的」压暗禁点**（含断 Tab；`ovOn` 类＋disabled）——点了＝关层的隐形二段操作废止。
- **判词在场开浮层＝判词自动收细签、关层还原**（旗标制，不覆盖用户手收态）；一屏一窗成立。
- 「我的」四子页（设置/六卷原文/见闻录/改名号）**同路往返**：backTo 挂 `overlayOnClose`，翻门透传，内部跳转先解除回调防连锁（openMine 亦补 plazaNavAway 堵孤儿浮层雷区）。
- 状态行随判词口径：判词期「第 n 掷待行/已定」，轮相牌题头「本掷/上一掷」随场切换。
- 数据补格：空無邊處天/佛佛 白话短键补齐（2210/2210 全覆盖）；`check:evidence` 改**缺白话格即失败**的硬校验。
- 顺带修复：`loadSave` 漏读 `cardTheme`——写经纸主题从此刷新不丢。

**第二批·色彩立宪**：见 §一.2a。判词卡「行 ▸」与掷轮钮双 CTA 经核验组裁定为 v360 定案（§七之七），不收敛。

**第三批·标签避让与天梯**：
- 「同族同进退」接线（原 auxFamOK 死代码）：辅标按族距离门整族显隐；**族表用 zh() 前原名查**（av.rawName），繁体模式不失配。
- 辅让主全局化：辅标避让改查全部节点矩形（跨节点连排如「增长天王＋七金山…」绝迹）。
- 保留导航区占位：右天梯（含顶底签 ±22px）/左档位簇/左探底杆可见时其 DOM 矩形先入避让集，标签不挤竖沟。
- 天梯门号与顶底签加四向暗晕 text-shadow（与 .nlabel.far 同法），窄屏亮底可辨。

**第四批·全屏页与字级**：
- 全屏页一根轴：`.fsShell` 收窄 680 与内容同轴（大厅 .pzShell 1180 不动）；顶条条目 gap:18px；窄屏模式卡动作词补「›」按钮特征；640px 藏 eyebrow 限定装饰字（共修动态数据行放行）；顶条相对时间加分钟桶（不再「刚刚」挂几小时）；「我的」失败态补重试钮（与大厅 #pzRetry 同构）。
- 散点字号 11–22px 全量归五档变量（大字模式自此全站生效）；豁免清单见 §一.4a。

**第五批·交互润饰**：
- `sfpPalmAbort()` 抽公用：endSfp/联机强制水合不再滞留「按住」金底与满格进度环。
- rolling 护栏改判：判词待行是静止态——重开类仅单机判词期放行（联机 resolving 期仍拦，免晾服务器）；轮相牌判词期放行看行迹。

**验证**：build ✓ 模拟300全圆满 ✓ crosscheck 220/220 ✓ evidence 2210 硬校验 ✓ engine 11 ✓ grant 83 ✓ ui 46 ✓ v90 31 ✓ net 34 ✓ room 37 ✓ plaza 42 ✓ plaza-client 19 ✓ name-ready 20 ✓ finish 8 ✓ backlang 17（新增判词返回键/同路往返/动画窗双按三回归段）✓；标签三组 DOM 断言（桌面/手机/繁体：远景族隐·近景族现）✓。

**对抗核验轮（8 智能体：4 分域审查＋逐指控核验，4 提 3 成立均 P3，已修）**：
① openCanon 主钮题「回到局中」而 backTo 使其落「我的」——改为 sfpS.active 时先解除返程回调（「关闭」态仍同路往返）；
② 掷轮动画窗（功课已计而判词未出约 1.3–2.1s）双按返回仍丢本掷——popstate 局中分支补 `rolling && !verdictFn` 守卫，toast「本掷正在落定，稍候」，backlang 补动画窗双按回归段；
③ 红族三废基座残留六处（#secZero/降位辉光/.smRow.arm/.sfpChip.cur/#secTrack/#sfpDice.settle）——全数归 176,90,66（--woe）基，降位大字辉光归 240,175,158（--woe-tx）基；UI CSS 红族自此确为两基座，token 注释「#8b3f32 归此」名实相符。

## 七之九 · 复刻 makeplay V99（2026-07-29 夜，上游 v387–v391 五迭代）

**增量范围**（V98→V99 md5 对比）：仅 game.js（+8.8KB）与 sfp-gloss.js（+3KB）改动＋新增两个生成数据层；sfp-data / sfp-canon / 白话两库 / zh-conv 全部未动（本地 87 处裁定修正与第一门校勘不受冲击）。

**已并入（映射到本地证据层单一取值口，非上游五处散改）**：
- **v389 字义解层** `src/sfp-glyph-why.js`（SFP_GLYPH_WHY，1369 格）：谱主未另作按语的 位×轮相，依卷首〈輪相表法第一〉六字定诠＋行法表去向补一层理解。红线守恒＝新证据类型 `glyph_interpretation` 另栏署「字义解」，不冠「譜曰」不署蕅益。接入点：证据层 `makeSfpGlyphEvidence`；判词 why() 闭包末位兜底（谱注→通例引文→字义解）；解读卡「字义」行；问答（缘由专答/组合专答）另署段；位卡行法表小字。
- **v390 总括句展开层** `src/sfp-refer-why.js`（SFP_REFER_WHY，387 格）：「餘如前說／餘皆例前可知」类谱注溯源到被指位同组按语——白话取被指位按语之白话（**优先本地 87 修正版**，经比对 387/387 与上游全同）＋尾注「所指即『某位』本组之注」；被指位逐字引文另列一条（新 source 子类型 `refer_note`，折叠与「阅读原文」落到被指位）。在 sfp-evidence.js 构建期一处接入，判词卡/解读卡/位卡/问答全端点自动生效。
- **v387 谱文页白话兜底**：rdCite 与 openCanon 谱文页对读，「全段零对读才垫白话」改「首段无对读即垫」且白话提至最前（白话→原文一序）；长文截断改「先按对读键后求句界」（圆五品位 232 字整段键在 229 字被切、白话全掉之修）。
- **v391 两修**：门总说卡为门梯让位（右置 64px＝6+门梯52+6，居中改右锚；640 宽视口点门四/七/十被卡吞、门珠隐去满屏空之修）；门场取景 rH/rV 分算（竖排门如色无色天二十一天不再顶天出画），＋只读探针 `__doorGeo`。上游 `#ladder.hush` 滚动锁未采（本地门梯 52vh 定高不滚动，规则不适用）。
- **sfp-gloss V99 整文件采纳**：门 3–14 导语改写为更贴谱文全文（毗婆沙五趣说、四洲福报/闻法两序、四空非方所、七逆与取相忏边界、四轮王统御诸洲、夹熏禅生五净居等）＋约 25 条名相由通行辞典释改为谱内自证（引本谱明文），開權顯實条带第三元素出处脚注（本地 openGloss 原生支持 g[2]）。「修夾熏禪。生五淨居」已核 B0136 卷三/卷五逐字；「佛表覺慧」已核卷三廣果问答逐字。
- **护栏契约同步**：check:evidence 新增两层永久校验（refer 387 格逐字＋被指位存在＋本格确为谱注；glyph 1369 格去向句式全解析、与行法表零差异、门内/跨门/门名全核、不冒谱曰、只入总括句格）；ontology `sourceSubtypes`+refer_note、+glyphInterpretation；门 3/5 导语快照钉随新文改关键判语。

**上游 VAGUE_FORMS 16 式已录入证据层**（含糊句白话被 refer 替换或补字义解；两层皆无的 49 格保留原句白话诚实呈现——上游选择留白，本地判保留更信息完整）。

**移植前独立核证**（scratchpad verify-v99-layers.mjs）：字义解 1369 格去向解析全覆盖、对行法表 **0 错**；总括展开 387 格引文皆被指位逐字原文、白话与本地校正版 387/387 全同；290 格谱注与字义解并存＝总括句格备兜底（显示层谱注优先，非覆盖）。

## 七之十 · 「我的」页排版收口（2026-07-30）

依 07-29 审计同一病理（用户点单「我的页面排版布局极简优化」）：①**数字只住一张卡**——「共修·连续·及第」从游离散行（.myLine，与四格卡两套视觉语言相邻）并入四格表底行（.myGrid .sub，grid-column:1/-1 自得同卡分隔线）；②**见闻行减负**（§5.0b）——「开局·升·沉·见」四数字与见闻录内页六格全数重复，收为「行谱见闻 · 本机 ｜ 见 N/220 位 ›」，明细归见闻录一处，小标题随之撤（行自题）；③**色归 token**（§一.2a）——我的页 8 个灰褐散点 hex 收编 --note/--paper/--gold/--gold-hi，.me 高亮 .05→.09，月历边框归 .16 同刻度，间距 12/18 两档。验证＝build 绿＋test:v90＋test:ui＋双视口截图 DOM 断言（.sub 并卡/.myLine 绝迹/标题唯「谱与设置」）。
同日提案《共修与纠错方案-20260730提案.md》：等候室情境主按钮与聊天全屏（W1-W4）·远端动态三级增强（R1-R3）·大厅茶寮接主站 /api/cmt（乙案）·报错/纠错系统（foyue reports 表＋admin 区块＋游戏五挂点）——均待用户点单。

## 七之十一 · 石青·晓与共修五批施工（2026-07-30，用户「开工」）

依《共修与纠错方案-20260730提案》定稿五批一次施工（F→B→C→D→E）：

- **F 石青·晓（色彩定案）**：`:root` 新增 `--aq-*` 浅面 token 族（页底 #e0e8ec/面板 #eff3f5/青墨 #23343c 11.6:1/次文 #47565d 6.8:1/泥金 #a8811f 只作线形不作小字/石绿 #2f6a5e/土红 #8b4a3a）；**社交与个人面**（大厅/共修动态/我的/茶寮/等候室面板含聊天/取名问密码卡）全部换石青浅底，**星图世界与局中控制台/判词/位卡等局中卡族仍暗夜**——同一窟壁画的明暗两面（暗夜矿彩在后，青底泥金展卷浮前）。浅面主钮＝金洗底＋青墨字＋金深描边；珠色点加 30% 青墨描边防发虚；数字用泥金深 --aq-strong。经四轮定向（甲夜提亮/乙半纸/丙宣纸皆废）后定石青，对比稿 artifact 9ccfbf01。
- **B 等候室**：W1 情境主按钮——独自在房时开局钮位＝「邀请莲友·点开即入」（§七明文落地），底行邀请钮同屏收起（§5.0b）；W2 ⤢ 全屏钮（桌面唯一放大手段，`.full` 移出媒体查询并同覆 max-height 钉子；手机 .full 补 width:100%）；W3 状态行只报人数、开局条件归指引句一次；等候/结算态手机默认全高抽屉（留缝可收，修宪见 §七聊天条）。
- **C 远端动态**：R1 远端掷轮借状态行播 5.2 秒（netPeerMsg，单槽避让：本人判词/棋讯在场只入消息回看）＋脉签该座色点闪一记（pdFlash）；R3″ 远端珠轻缓呼吸脉动（±6%、2.2s 周期，离线珠不闪只淡显 45%，prefers-reduced-motion 静止，不入 netBusy 不烧帧）；R2 乘光弧线撤销（用户「不过度设计」）。
- **D 莲友茶寮**：大厅两卡改三卡（一人行谱｜与人共修｜茶寮，窄屏三枚留形+题）；茶寮＝第四张全屏页（修宪见 §七之六），接佛悦主站 `/api/cmt` 同群共数据（6 秒增量轮询、10 分钟时间戳、150 字、快语两枚、首发言走取名卡草稿不丢、公告 textContent 呈现）；dev 用合规新 id `g-`+24hex（practiceId 带下划线不合主站校验）；本地调试 `sm10.fy.base` 可指本地 worker。
- **E 纠错系统**：统一上报卡 openReportCard（四类 chips＋定位串程序附带＋≤300 字，POST foyue `/api/report`）；五挂点＝判词卡释义行尾「纠错」小字、位卡底行钮、谱文页页脚「本门纠错」（整门级，不逐位加钮免与「位卡›」争行）、解读卡底行钮、问答每条答复尾角标（data-qi 回取问句）。foyue 侧同批建成：reports 表＋POST /api/report（REPORT_RL 2/分）＋admin「报错/纠错」区块（open/done/ignored 状态机，esc 转义硬要求）＋ /api/cmt 套现成 CORS（withCors，ALLOW_ORIGIN 本含 *.foyue.org）。

**同日第二轮（用户四点定案）**：
- **聊天室只留人语与人事，行棋归状态行＋消息回看**：远端掷轮/贈掷施与/贈掷作废等行棋说明一律不进房间聊天（net.js 撤 toss 系统行、贈掷三条改走新钩子 `Net.onNotice` → 状态行 netPeerNotice）；聊天内仍留「本局共同开局／超时暂离／归队」等人事行。
- **安卓滑不动病根修复**：全局 `touch-action:none` 从 html/body/#app 收窄为仅画布（自定手势），根层改 `pan-x pan-y`（双击放大与捏合仍禁）；各滚动区（.fsBody/.overlay .body/#netMsgs/.clMsgs/.pzMain）补显式 `touch-action:pan-y`——安卓 Chrome 会把祖先的 none 连累到浮层内滚动，iOS 不严格执行故此前只在安卓复现。
- **茶寮：常驻说明句撤**，改「初到之约」一次性小签（clHello，本机记 sm10.cl.hello，「知道了」即收）；快语加第三枚「请问这一局怎么玩？」（引新手发问）。茶寮定位＝全站莲友歇脚处（问路·随喜·轻谈），治理沿用主站闭环（频控/屏蔽词/封禁/删言/公告）；后续候点单：及第随喜自动入茶寮、@名号轻提及、未读浮标。
- **石青底字色**：亮度背景定案；字主色出三方案对比稿（甲青墨现状/乙沉香暖墨·荐/丙黛绿墨，artifact 9ccfbf01 v5），定案后 `--aq-tx/--aq-title/--aq-note` 三 token 换值即生效。
- 测试口径随新契约同步四处（name-ready 独房邀请主按钮×2、ui 开局条件归指引句×2）；无头断帧环境下非 force 点击/waitFor(visible) 会悬死属环境怪癖（rAF 长窗不来，截图可催帧），应用行为经直发 click 探针验证无恙。

## 七之十二 · 青纸墨书泥金题＝全局定案＋第三轮五点（2026-07-30）

- **字色定案「青纸墨书泥金题」并升为全局卡面语言**：①`--aq-*` 墨族换沉香暖墨（正文 #39322a 11.3:1 / 题字 #4a3d28 / 次文 #5f574a 6.3:1；结构线与洗底随墨转暖 rgba(112,96,64,.26)/rgba(57,50,42,.05-.08)），泥金/石绿/土红不动——字与泥金同暖族，青底作衬，冷暖相济即生机；②**卡片默认主题改「青纸」**（原写经纸暖麻纸调整体转青纸：--ck-panel #f2f6f7→#e0e8ec 渐变、题沉香、顶签泥金深 #8a6414、链接土红、原文凹槽青纸深一档、我方气泡金洗；save.cardTheme 默认 'paper'，设置钮题「青纸/暗夜」，暗夜可切回）——位卡/判词卡/解读卡/问/谱文页/玩法卡等全部卡族与大厅/我的对齐，星图世界不变（暗夜矿彩在后，青纸展卷浮前）。
- **纠错收口**（用户令「很小的辅助功能」）：判词弹窗、位卡、谱文页三处入口撤；只留 AI 解读卡底行「纠错」＋问答每条答复角标；上报卡极简为「四签＋一框＋一钮」，定位串只随报文送出不占卡面。
- **称念补全**（用户令）：①「至心称念＋六字」自按住挂到**轮停定相**才退场（原松手即清——快速点按者一闪而过，形同首掷无称念）；②六字白话名号**一行不折**（#sfpChant nowrap＋字号 min(29px,7.4vw) 窄屏收字不折行）。
- **发热治理追加**：全屏页（大厅/动态/我的/茶寮）盖住星图时画布降至约 4fps 空转（frame 头部 overlay .pzPanel 判定；转场/飞行放行，关页即恢复）——被盖住仍整帧渲染是发热一大来源；与 §七之二 既有四招（触屏 DPR≤1.6/泛光减半/静观 30fps/自适应降档）并行。
- **茶寮扩容规划（内容多起来之后，候点单）**：近期＝未读浮标＋「回到最新」（借鉴 netNew）、同人连发折叠（「某某又说了 N 句 ›」）、DOM 上限已 220 条；中期＝「翻早先」按需上翻（foyue /api/cmt 增 before 参数）、消息流分片（全群｜只看游戏，用现成 ep 字段作视图筛选不动主流）、高峰慢速模式（频控随在线数收紧）；远期＝旧留言 D1 定期归档（admin 侧导出）、精华置顶（notice 已可用）。原则不变：一个群不分裂、治理在主站一处。

## 七之十三 · 「我的」页极简收束（2026-07-30 晚，用户点单）

用户令「重新布局极简高级：行谱记录可不要，谱与设置按钮重新规划」。在 §七之十 收口的基础上再减一层：

- **撤「我的行谱记录」清单**：`.myRuns`/`.myRun` 连同数据渲染全撤（近局明细见闻录与及第结算卡已各有一处，此处属重复陈列）；
- **撤两条分节小题**（`.myLine2`「我的行谱记录」「谱与设置」）——分节靠卡自身边界，不靠字标；
- **三行收进一张分组卡 `.myList`**：行谱见闻／六卷原文／设置三行同住一张发丝线圆角卡（`--aq-line` 1px 分隔、圆角 12、行内钮去自框），与四格卡、月历同一卡语言；行题减字（「· 本机」「· 逐字读谱」「· 声音 · 简繁 · 卡片」等副词全去），右侧只留数字或 ›。
- **页面终态六段**：名号行 → 四格卡 → 月历卡 → 三行卡 → 一句注 → 一枚主钮，通页三张卡零散行。

验证＝build 绿＋test:v90 31＋test:backlang 17＋test:ui 46＋双视口截图 DOM 断言 16/16（.myList 三行行序／myLine2·myRun 绝迹／行题减字／卡圆角行去框，/tmp/mine-min-shots）。#myLg/#myCanon/#mySet 三 id 未动，既有测试与返程链路（子页同路往返「我的」）不受影响。

## 七之十四 · 共修两处卡死勘正（2026-07-31，用户报）

### 一、联机成佛后一直卡在转场里

**成因**：联机的「共同结算」常在本座这一手**乘光途中**送达——末位及第时服务器 `advanceTurn` 见 `finishing && nextIdx===0` 当即 `finishMatch`，`match_finished` 往往在飞行的头几百毫秒就回来。`sfpVictory(true)` 立刻把 `sfpS.active` 置 false，而在途的三处回调（`doSkip` 内层落位、谱页 `fadeTransit` 回调、`sfpGoto` 起彗星前的延时）当时都是「见 `!sfpS.active` 即 `return`」——一退走人，转场页 `#fadeWhite` 就永远停在满不透明。它 z-40 高于面板 z-30，及第卡开在底下也看不见，人只剩一屏星河。

**修**：
- 新增 `clearTransit()` 一处收口（彗星＋直达钩子＋转场页＋`sfpTransit` 一并归零），`startSfp`/`endSfp`/`sfpVictory` 与三处在途回调全部改走它；
- 根因层：`sfpVictory` **先候本手落定再收局**（`sfpTransit` 期间每 300ms 重试，六秒仍不落定则强收兜底）——动画完整播完，及第卡照常弹出。

**验证**：以 `moveFx:false` 走直达链路，在转场页拉满的那一拍收局——修前 `#fadeWhite` opacity 恒为 `1`（复现），修后恒回 `0`，掷轮钮亦不再卡在「行棋中…」。

### 二、掷轮后点「全图」跳到别的门重新掷轮

**成因**：`sfpSave()` 遇 `Net.active` 即早退（联机棋况只认服务器快照），但「全图」（顶栏钮与题字入口）一律 `endSfp('行处已存…')`。于是共修局中一点全图：本机局收了、轮次却照跑（人不在局白等超时被跳手），底坞钮又读到**上一局单机存局**显「续掷」，一点就把人送回上局停的那一门从头掷——用户所见「跳到 12 门开始掷轮」即此（12 门只是旧存局恰停在別教位次门）。

**修**：
- `netSeatedInPlay()`（在座且本局 playing）时，「全图」**只拉远观照、不收局**——出门观＋拉远＋一句「本局仍在，轮到您时照常掷轮」；毕局位分支同理（及第者留座待结算）。
- 新增 `netRejoin()`（挂 `hydrateNetGame(true)`）：底坞主钮与题屏两枚主钮在座时**一律按服务器快照回位**，不落到本机存局；底坞钮在共修局中改题「回局中」。
- `Net.onToss` 兜底：若本座已不在局又轮到自己，先按 `steps[0].from`（服务器给的本掷起手位）回位再演这一掷——否则新开的空局会把本掷当首掷，人被打回门一「發始因地」重新起行。

**验证**：双客户端对本地 `wrangler dev` 实跑同一房——修前点全图后掷轮栏消失、底坞显「续掷」、一点即落到预置旧存局「六不退心」（门十二）；修后掷轮栏仍在、局仍属本座，回局落在服务器行处。

验证总账＝build 绿＋test:v90 31＋test:ui 46＋test:finish 8＋test:backlang 17＋test:engine／test:room／check:grant／check:evidence／check:dir 全绿。
